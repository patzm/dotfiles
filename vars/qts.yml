---
user_group: administrators

pip3_args: "--global-option=build_ext --global-option='-L/opt/lib:/opt/lib/gcc' --global-option='-I/opt/include/'"
pip3_bin: /opt/bin/pip3

vim_distros:
  - vim

env:
  PATH: "/opt/bin:/opt/sbin:{{ ansible_env.PATH }}"
