---
- name: installing core packages
  become: yes
  package: name={{ item }} state=latest
  with_items:
    - htop
    - xclip
    - git
    - gnome-keyring
    - libsecret
    - vim
    - neovim
    - zsh
    - ranger
    - unrar
    - curl
    - wget
    - bmon
    - feh
    - python2-pip
    - python-pip  # Python 3
    - wmctrl
    - imagemagick
    - hsetroot
    - exiv2
    - tree
    - tmux
    - neofetch
    - rxvt-unicode
    - rxvt-unicode-terminfo
    - urxvt-perls
    - deepin-calculator

- name: copy yay config files
  copy:
    src: '{{ role_path }}/files/archlinux/yay/config.json'
    dest: '{{ yay_config_file }}'
    owner: "{{ ansible_env.USER }}"
    group: "{{ user_group }}"
    force: yes

- name: install powerline fonts
  command: yay -S powerline-fonts-git

- name: install Python3.7
  command: yay -S python37

