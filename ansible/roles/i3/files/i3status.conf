
order += 'sysdata'
order += 'nvidia_temp'
order += 'whatismyip'
order += 'netdata'
order += 'keyboard_layout'
order += 'clock'

clock {
    format = ['Muc {Europe/Berlin}', 'Valley {America/Los_Angeles}']
    format_time = '{timezone} %Y-%m-%d %H:%M:%S'
}

keyboard_layout {
    layouts = ['us', 'de']
}

whatismyip {
    format = 'IP: {ip}'
}

