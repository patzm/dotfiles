githubUsername: patzm
externalGitArgs: |
    clone:
        args:
            - --depth
            - "1"
    pull:
        args:
            - --depth
            - "1"
xdgRelDataDir: .local/share
xdgRelConfigDir: .config
